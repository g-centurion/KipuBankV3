###############################################
# KipuBankV3_TP4 - Example environment file   #
# Copy to `.env` and fill with real values.   #
###############################################

# ========== REQUIRED ==========
# Private key of deployer (0x-prefixed)
# WARNING: never commit your real .env to git
PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# Sepolia RPC (Alchemy/Infura/QuickNode/etc.)
RPC_URL_SEPOLIA=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Etherscan API key (for verification)
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_API_KEY

# Chain id for Sepolia
CHAIN_ID=11155111

# ========== OPTIONAL ==========
# Deployer address (derived from PRIVATE_KEY)
# DEPLOYER_ADDRESS=0xYourDeployerAddress

# Foundry profile
# FOUNDRY_PROFILE=default

# ========== USAGE (WSL) ==========
# cd /home/sonic/KipuBankV3_TP4
# source .env
# ~/.foundry/bin/forge script script/Deploy.s.sol:DeployScript \
#   --rpc-url "$RPC_URL_SEPOLIA" -vvvv

# Notes:
# - Always run from WSL (not PowerShell) to avoid env expansion issues.
# - Keep `.env` out of git; only `.env.example` is committed.
